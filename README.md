# AI Менеджер Фаст-фуда (WhatsApp Bot)

Этот бот автоматизирует прием заказов в WhatsApp для фаст-фуд ресторана. Он использует AI (Perplexity/OpenAI) для общения с клиентами и Whapi.cloud API для отправки сообщений.

## 📋 Требования

- Python 3.8+
- Аккаунт Whapi.cloud (для WhatsApp API)
- API ключ Perplexity (или OpenAI)

## 🚀 Установка

1. **Клонируйте репозиторий** (или скачайте файлы):
   ```bash
   git clone <ваша-ссылка-на-github>
   cd botai
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройка окружения**:
   Создайте файл `.env` (если его нет) и добавьте ваш ID телефона (не обязательно, если прописан в коде):
   ```env
   WHATSAPP_PHONE_ID=77071234567
   ```
   *Примечание: API ключи сейчас прописаны в `bot.py`, для безопасности лучше вынести их в `.env`.*

## ▶️ Запуск

Запустите бота командой:
```bash
python bot.py
```
Бот начнет опрашивать сервер Whapi каждые 3 секунды на наличие новых сообщений.

## 📤 Как загрузить на GitHub

Если вы хотите сохранить код на GitHub, выполните следующие команды в терминале (в папке с проектом):

1. **Инициализация Git** (если еще не делали):
   ```bash
   git init
   ```

2. **Добавление файлов**:
   ```bash
   git add .
   ```

3. **Создание коммита** (сохранение изменений):
   ```bash
   git commit -m "Initial commit: Polling bot version"
   ```

4. **Связь с GitHub**:
   - Создайте новый репозиторий на сайте GitHub.
   - Скопируйте ссылку на него (например, `https://github.com/username/repo.git`).
   - Выполните команду (замените ссылку на свою):
   ```bash
   git remote add origin https://github.com/username/repo.git
   ```

5. **Отправка кода**:
   ```bash
   git branch -M main
   git push -u origin main
   ```
